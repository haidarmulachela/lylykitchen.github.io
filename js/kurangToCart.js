
function kurangToCart(jumlah, itemName, price) {
	let cartItems=[]
    let cartTotal=0.00
            
			if (jumlah!=0){
            jumlah--;
    const indexToRemove=cartItems.findIndex(item=> id === id);

    cartTotal -= price;
		}
			else{
		}
        console.log(jumlah,"kue"+itemName,"harga"+price)
            
}
module.exports=kurangToCart
kurangToCart("2","aaa",2000)